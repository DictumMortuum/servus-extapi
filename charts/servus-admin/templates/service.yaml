apiVersion: v1
kind: Service
metadata:
  name: {{ include "servus-admin.fullname" . }}
  labels:
    {{- include "servus-admin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servus-admin.selectorLabels" . | nindent 4 }}
