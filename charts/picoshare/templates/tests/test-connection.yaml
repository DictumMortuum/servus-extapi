apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "picoshare.fullname" . }}-test-connection"
  labels:
    {{- include "picoshare.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "picoshare.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
