apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nginx-autoindex.fullname" . }}-test-connection"
  labels:
    {{- include "nginx-autoindex.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nginx-autoindex.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
